$(function(){

  // Reviews Slider
  let slider = tns({
    container: '.reviews-slider',
    items: 2,
    slideBy: 'page',
    autoplay: true,
    gutter: 70,
    navPosition: 'bottom',
    navContainer: '.reviews-slider_nav',
    mouseDrag: true
  });

  
});